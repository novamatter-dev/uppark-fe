import { StyleSheet } from "react-native";

const KeyboardViewStyle = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  container: {
    backgroundColor: "#EBEBEB",
    padding: 24,
    flex: 1,
    justifyContent: "space-around",
  },
});

export default KeyboardViewStyle;
